{
  "name": "melato-ai-concierge",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-cost Melato AI customer service concierge for website chat, email reply drafting, browser voice mode, PWA, Shopify embed, and Android export.",
  "scripts": {
    "check": "node --check embed/melato-concierge-widget.js && node --check assets/app.js && node --check scripts/build.js",
    "build": "node scripts/build.js",
    "start": "npx serve .",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "android:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "serve": "^14.2.4"
  }
}
